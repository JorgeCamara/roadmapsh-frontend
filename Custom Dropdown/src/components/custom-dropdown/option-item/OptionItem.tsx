function OptionItem () {
    return (
        <li
            /* key={option.key}
            onClick={() => props.onOptionSelected(option)}
            className={styles.optionListItems} */
        >
            {}
        </li>
    );
}

export default OptionItem;